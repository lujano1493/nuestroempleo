<p class="nowrap">
  <a href="/mis_ofertas/{{= it.slug }}/carpeta" data-item-id="{{=it.id }}" class="folder-name">{{= it.nombre }}</a>
</p>
<a href="#" data-action-role='open-in-table' data-table-prop='#tmpl-acciones'>Acciones</a>
