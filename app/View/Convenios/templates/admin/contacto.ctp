<p>
  <span class="block">{{= it.admin.nombre }}</span>
  <span class="block">
    <strong>{{= it.admin.email }}</strong>
  </span>
  <span class="block">
    <small>Tel: {{= it.admin.tel }}</small>
  </span>
</p>