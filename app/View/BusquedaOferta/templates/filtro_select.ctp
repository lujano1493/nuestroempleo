<p class="filtro-select ellipsis" style="width:150px;display:inline">
	<span class="badge badge-info">
		 <button type="button" class="delete-filter" data-value="{{=it.value_query}}" data-query="{{=it.field_query}}"  >×</button>
		<a href="#" class="{{=it.field_query}}" title="{{=it.value_query}}"  data-placement="bottom"  data-component="tooltip" >
			{{=it.value_query}}
		</a>
	</span>
</p>